{
  "name": "mediguide-ai",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev -- --hostname 127.0.0.1",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "start": "npm --prefix frontend run start"
  }
}
